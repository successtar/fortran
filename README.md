# fortran 95 coding

Simple Guess Game with fortran. It accept two input, compare them and change the level based on the comparison result

The code can be tested online here https://www.tutorialspoint.com/compile_fortran_online.php

Provide inputs to the code via the STDIN tab on the page
